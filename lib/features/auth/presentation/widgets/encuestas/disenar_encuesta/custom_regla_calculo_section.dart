import 'package:flutter/material.dart';

class CustomReglaCalculoSection extends StatelessWidget {
  const CustomReglaCalculoSection({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
